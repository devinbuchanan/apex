# Apex Repository Guidelines

- Run `swift test` or `xcodebuild test` before committing if possible.
- Follow Apple's Human Interface Guidelines and the included Liquid Glass documentation when updating UI.
